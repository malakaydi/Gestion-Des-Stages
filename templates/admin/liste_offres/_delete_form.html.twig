<form method="post" action="{{ path('liste_offres_delete', {'id': liste_offres.id}) }}"
 onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ liste_offres.id) }}">
    
</form>
