{% extends 'base.html.twig' %}

{% block title %}Departements{% endblock %}

{% block body %}
    <h1>Departements</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ departement.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ departement.titre }}</td>
            </tr>
        </tbody>
    </table>



    <a href="{{ path('departements_edit', {'id': departement.id}) }}">edit</a>

    {{ include('admin/departements/_delete_form.html.twig') }}
{% endblock %}
