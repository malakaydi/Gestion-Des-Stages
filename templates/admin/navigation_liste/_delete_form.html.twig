<form method="post" action="{{ path('navigation_liste_delete', {'id': navigation_liste.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ navigation_liste.id) }}">
    <button class="btn">Delete</button>
</form>
