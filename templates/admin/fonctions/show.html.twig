{% extends 'base.html.twig' %}

{% block title %}Fonctions{% endblock %}

{% block body %}
    <h1>Fonctions</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ fonction.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ fonction.titre }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ fonction.status ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('fonctions_index') }}">back to list</a>

    <a href="{{ path('fonctions_edit', {'id': fonction.id}) }}">edit</a>

    {{ include('fonctions/_delete_form.html.twig') }}
{% endblock %}
